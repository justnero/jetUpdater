apply plugin:'java'

sourceCompatibility = "1.7"
targetCompatibility = "1.7"

repositories {
   flatDir {
       dirs 'lib'
   }
}

dependencies {
    compile fileTree(dir: 'lib', include: '*.jar')
}

jar {
    manifest {
        attributes 'Main-Class': "ru.justnero.jetupdater.Main"
    }
}